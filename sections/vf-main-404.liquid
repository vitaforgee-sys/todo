<section class="vf-section">
  <div class="vf-wrap vf-empty">
    <h1>No encontramos esta pagina</h1>
    <p class="vf-lede" style="margin-inline: auto;">
      Puede que el enlace haya cambiado de sitio. Te dejamos por donde seguir.
    </p>
    <div class="vf-btn-row" style="justify-content: center;">
      <a href="{{ routes.all_products_collection_url }}" class="vf-btn vf-btn--primary">Ver los productos</a>
      <a href="{{ routes.root_url }}" class="vf-btn vf-btn--outline">Volver al inicio</a>
    </div>
  </div>
</section>

{% schema %}
{ "name": "Pagina 404", "tag": "section", "settings": [] }
{% endschema %}
